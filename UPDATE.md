# Как обновить бота после добавления Boosty

После интеграции Boosty нужно обновить бота на сервере.

## 🚀 Быстрое обновление

### 1. Обновите .env на сервере

```bash
ssh your-user@your-server-ip
cd ~/english-bot
nano .env
```

Добавьте новые переменные:
```env
# Добавьте эти строки в конец файла .env
BOOSTY_PAYMENT_LINK=https://boosty.to/yourpage/goals/12345
BOOSTY_WEBHOOK_SECRET=your_secret_key_if_any
WEBHOOK_PORT=3000
```

Сохраните: `Ctrl+X`, `Y`, `Enter`

### 2. Запушьте изменения (локально)

```bash
git add .
git commit -m "Add Boosty integration"
git push origin main
```

GitHub Actions автоматически задеплоит на сервер.

### 3. Проверьте, что всё работает

```bash
ssh your-user@your-server-ip
pm2 logs english-bot
```

Должна быть строка:
```
🌐 Webhook сервер запущен на порту 3000
```

### 4. Откройте порт 3000 (если webhook используется)

```bash
sudo ufw allow 3000/tcp
sudo ufw status
```

## 🔍 Проверка

### Проверьте статус бота:
```bash
pm2 status english-bot
```

### Проверьте webhook сервер:
```bash
curl http://localhost:3000/health
```

Должен вернуть:
```json
{"status":"ok","bot":"running"}
```

### Проверьте логи:
```bash
pm2 logs english-bot --lines 50
```

## ❌ Если что-то пошло не так

### Бот не запускается:

```bash
cd ~/english-bot
npm install
npm run build
pm2 restart english-bot
pm2 logs english-bot
```

### Порт 3000 занят:

Измените порт в `.env`:
```env
WEBHOOK_PORT=3001
```

И откройте новый порт:
```bash
sudo ufw allow 3001/tcp
pm2 restart english-bot
```

### Webhook не работает:

Используйте ручной режим:
1. Получите уведомление о платеже от Boosty
2. Найдите комментарий вида `USER_123456789`
3. Отправьте боту: `/paid 123456789`

## ✅ Готово!

Теперь бот принимает платежи через Boosty! 🎉

**Тестирование:**
1. Запустите бота: `/start`
2. Пройдите до оплаты
3. Проверьте ссылку на Boosty
4. Сделайте тестовый платёж

**Команды для управления:**
- `pm2 status` - статус
- `pm2 logs english-bot` - логи
- `pm2 restart english-bot` - перезапуск
- `/paid USER_ID` - выдать доступ вручную
