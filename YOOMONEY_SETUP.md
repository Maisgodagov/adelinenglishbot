# Подробная инструкция по настройке ЮMoney (2025)

Полное руководство по подключению приёма платежей через ЮMoney к вашему Telegram-боту с автоматической выдачей доступа.

---

## 📋 Содержание

1. [Что такое ЮMoney и зачем оно нужно](#что-такое-юmoney)
2. [Регистрация и идентификация кошелька](#шаг-1-регистрация-и-идентификация)
3. [Настройка HTTP-уведомлений (webhook)](#шаг-2-настройка-http-уведомлений)
4. [Настройка сервера и домена](#шаг-3-настройка-сервера)
5. [Настройка бота](#шаг-4-настройка-бота)
6. [Тестирование платежей](#шаг-5-тестирование)
7. [Troubleshooting](#troubleshooting)

---

## 🎯 Что такое ЮMoney?

[ЮMoney](https://yoomoney.ru/) (бывший Яндекс.Деньги) — российская платёжная система, которая позволяет принимать платежи от пользователей.

### Почему ЮMoney?

**Преимущества:**
- ✅ **Низкая комиссия:** 2% с банковских карт, 0.5% с кошельков
- ✅ **Не нужно ИП/ООО:** физлица могут принимать платежи
- ✅ **Автоматические уведомления:** webhook для автоматической выдачи доступа
- ✅ **Моментальное зачисление:** деньги приходят сразу
- ✅ **Работает с российскими картами:** Visa, MasterCard, МИР
- ✅ **Простая интеграция:** Quick Pay форма, без сложного API

**Минусы:**
- ❌ **Нужна идентификация:** паспорт или Госуслуги (1-2 дня)
- ❌ **Требует HTTPS:** нужен домен с SSL сертификатом
- ❌ **Лимиты:** до 500,000₽/месяц для физлиц

### Альтернативы

Если ЮMoney не подходит:
- **YooKassa (ЮKassa)** — для ИП/ООО, более надёжная
- **Telegram Payments** — встроенная оплата в боте
- **Робокасса** — старая, но проверенная система

---

## 🚀 Шаг 1: Регистрация и идентификация

### 1.1 Создание кошелька (5 минут)

1. **Откройте сайт ЮMoney**
   - Перейдите на [yoomoney.ru](https://yoomoney.ru/)
   - Нажмите кнопку **"Войти"** в правом верхнем углу

2. **Выберите способ входа**

   Можно войти через:
   - **Телефон** (рекомендуется) — быстрее всего
   - **Email** — если нет российского номера
   - **Яндекс ID** — если есть аккаунт Яндекса

3. **Подтвердите регистрацию**
   - Введите код из СМС (если через телефон)
   - Или код из письма (если через email)
   - Придумайте пароль

4. **Заполните профиль**
   - Имя и фамилия
   - Дата рождения
   - (Это потребуется для идентификации)

**✅ Готово!** Кошелёк создан, но ещё не может принимать платежи.

---

### 1.2 Идентификация кошелька (обязательно!)

**⚠️ Важно:** Без идентификации вы НЕ сможете принимать платежи!

#### Способ 1: Через Госуслуги (самый быстрый, 5 минут)

Если у вас есть подтверждённая учётная запись на Госуслугах:

1. В личном кабинете ЮMoney найдите баннер **"Пройдите идентификацию"**
2. Нажмите **"Идентифицировать кошелёк"**
3. Выберите **"Через Госуслуги"**
4. Авторизуйтесь на Госуслугах
5. Подтвердите передачу данных
6. **Готово!** Идентификация проходит мгновенно

#### Способ 2: Онлайн по паспорту (1-2 дня)

Если нет Госуслуг:

1. Выберите **"Загрузить паспорт"**
2. Сфотографируйте:
   - Разворот с фото
   - Разворот с регистрацией
3. Сделайте селфи с паспортом
4. Отправьте на проверку
5. Ждите подтверждения (обычно 1-2 дня, иногда быстрее)

#### Способ 3: Через партнёрский банк

Если у вас счёт в:
- Сбербанк
- ВТБ
- Альфа-Банк
- Тинькофф
- И других партнёрах

Можете идентифицироваться через банк (мгновенно).

**💡 Совет:** Используйте Госуслуги — это самый быстрый способ!

---

### 1.3 Узнайте номер кошелька

После создания кошелька:

1. Откройте главную страницу [yoomoney.ru](https://yoomoney.ru/)
2. Номер кошелька отображается **вверху страницы** рядом с вашим именем
3. Формат: `410011234567890` (15 цифр, начинается с 4100)
4. **Скопируйте номер** — он понадобится для настройки бота

**Где найти:**
```
┌─────────────────────────────────────┐
│ Иван Иванов  410011234567890  ⚙️    │ ← Вот здесь
└─────────────────────────────────────┘
```

---

## 🔔 Шаг 2: Настройка HTTP-уведомлений (webhook)

HTTP-уведомления — это автоматические сообщения от ЮMoney на ваш сервер о каждом платеже. Именно они позволяют автоматически выдавать доступ к курсу.

### 2.1 Найдите настройки HTTP-уведомлений

**Вариант 1: Через меню**

1. Откройте [yoomoney.ru](https://yoomoney.ru/)
2. Нажмите на **иконку профиля** (⚙️) в правом верхнем углу
3. Выберите **"Настройки"**
4. В левом меню найдите **"Уведомления"**
5. Кликните на **"HTTP-уведомления"**

**Вариант 2: Прямая ссылка**

Просто откройте: [yoomoney.ru/transfer/myservices/http-notification](https://yoomoney.ru/transfer/myservices/http-notification)

---

### 2.2 Настройка формы HTTP-уведомлений

Вы увидите форму с полями. Заполните её так:

#### 📍 **1. Адрес для уведомлений**

```
https://yourdomain.com/webhook/yoomoney
```

**Замените `yourdomain.com` на ваш реальный домен!**

Примеры:
- ✅ `https://mybot.ru/webhook/yoomoney`
- ✅ `https://bot.example.com/webhook/yoomoney`
- ❌ `http://mybot.ru/webhook/yoomoney` — без HTTPS не работает!
- ❌ `http://localhost:3000/webhook/yoomoney` — localhost недоступен из интернета

**⚠️ Критично важно:**
- URL **ДОЛЖЕН** начинаться с `https://` (не http!)
- URL **ДОЛЖЕН** быть доступен из интернета
- Порт только 443 (стандартный для HTTPS)

**Нет домена?** Используйте ngrok для тестирования (см. раздел "Тестирование без домена")

---

#### 🔐 **2. Секрет для формирования подписи**

Это пароль, который будет использоваться для проверки подлинности уведомлений.

**Придумайте надёжный секрет, например:**
```
MyS3cr3tK3y_2025!
```

**Требования:**
- Минимум 8 символов
- Используйте буквы, цифры, спецсимволы
- Лучше генерировать случайно

**💡 Совет:** Сгенерируйте случайный пароль:
```bash
openssl rand -base64 32
# Пример вывода: Kd9mP2vL8qR5tYnWx3jF6hG9
```

**⚠️ ВАЖНО:** Сохраните этот секрет! Он понадобится в `.env` файле.

---

#### ✅ **3. Галочка "Уведомлять при переводе на кошелёк"**

Обязательно поставьте галочку:
- ✅ "Уведомлять при переводе на кошелёк"

Это активирует отправку уведомлений.

---

#### 💾 **4. Сохраните настройки**

Нажмите кнопку **"Сохранить"** внизу страницы.

**Что вы увидите после сохранения:**

```
✅ HTTP-уведомления включены
📍 Адрес: https://yourdomain.com/webhook/yoomoney
🔒 Секрет: ********** (скрыт)
```

---

### 2.3 Проверка настроек

После сохранения:

1. **Обновите страницу** с настройками HTTP-уведомлений
2. Убедитесь, что:
   - ✅ Статус: "Включено"
   - ✅ URL сохранён правильно
   - ✅ Есть галочка на уведомлениях

**❌ Если кнопки "Сохранить" нет:**

Возможные причины:
- Кошелёк не идентифицирован → пройдите идентификацию (Шаг 1.2)
- URL некорректный → проверьте формат (должен быть HTTPS)
- Уже настроено → проверьте, может уже всё готово

---

## 🌐 Шаг 3: Настройка сервера

Теперь нужно настроить сервер, чтобы он мог принимать уведомления от ЮMoney.

### 3.1 Требования к серверу

Для работы webhook нужен:
- ✅ **Домен с HTTPS** (обязательно!)
- ✅ **Открытый порт 443** (стандартный для HTTPS)
- ✅ **SSL сертификат** (Let's Encrypt бесплатный)

---

### 3.2 Вариант А: У вас есть домен (рекомендуется)

#### Шаг 3.2.1: Настройте DNS

Добавьте A-запись для вашего домена:

```
Тип: A
Имя: @ (или bot, или любое)
Значение: 123.45.67.89 (IP вашего сервера)
TTL: 3600
```

Проверьте командой:
```bash
ping yourdomain.com
# Должен пинговаться ваш сервер
```

#### Шаг 3.2.2: Установите SSL сертификат

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Установите Certbot
sudo apt update
sudo apt install certbot python3-certbot-nginx -y

# Получите SSL сертификат
sudo certbot --nginx -d yourdomain.com

# Следуйте инструкциям Certbot
```

Certbot автоматически:
- Получит бесплатный SSL сертификат
- Настроит Nginx
- Настроит автоматическое обновление

**Проверка:**
```bash
curl https://yourdomain.com/health
# Должно вернуть: {"status":"ok","bot":"running"}
```

#### Шаг 3.2.3: Настройте Nginx

Создайте конфиг для бота:

```bash
sudo nano /etc/nginx/sites-available/bot
```

Вставьте:

```nginx
server {
    listen 443 ssl http2;
    server_name yourdomain.com;

    # SSL сертификаты (автоматически созданы Certbot)
    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;

    # Настройки SSL (рекомендуемые)
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';

    # Webhook для ЮMoney
    location /webhook/yoomoney {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Страница успеха после оплаты
    location /payment/success {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Health check
    location /health {
        proxy_pass http://localhost:3000;
    }
}

# Редирект с HTTP на HTTPS
server {
    listen 80;
    server_name yourdomain.com;
    return 301 https://$server_name$request_uri;
}
```

Активируйте конфиг:

```bash
# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/bot /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Если всё ОК, перезагрузите Nginx
sudo systemctl reload nginx
```

**Проверка:**
```bash
curl https://yourdomain.com/health
# Должно вернуть: {"status":"ok","bot":"running"}
```

---

### 3.3 Вариант Б: Тестирование без домена (ngrok)

Если у вас нет домена, можно использовать [ngrok](https://ngrok.com/) для тестирования.

**⚠️ Важно:** ngrok подходит ТОЛЬКО для тестирования! Для продакшена нужен настоящий домен.

#### Установка ngrok:

```bash
# Зарегистрируйтесь на ngrok.com и получите токен

# Установите ngrok
npm install -g ngrok

# Авторизуйтесь (замените на ваш токен)
ngrok authtoken YOUR_AUTH_TOKEN

# Запустите туннель на порт 3000
ngrok http 3000
```

Вы получите URL вида:
```
https://abc123xyz.ngrok-free.app
```

Используйте его в настройках ЮMoney:
```
https://abc123xyz.ngrok-free.app/webhook/yoomoney
```

**Минусы ngrok:**
- ❌ URL меняется при каждом перезапуске
- ❌ Нужно обновлять настройки в ЮMoney
- ❌ Не подходит для продакшена

---

## 🤖 Шаг 4: Настройка бота

### 4.1 Обновите .env файл

Подключитесь к серверу и откройте `.env`:

```bash
ssh your-user@your-server-ip
cd ~/english-bot
nano .env
```

Добавьте настройки ЮMoney:

```env
# Основные настройки бота
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
CHANNEL_ID=-1001234567890
CHANNEL_LINK=https://t.me/yourchannel
SUPPORT_CONTACT=@your_support
COURSE_LINK=https://example.com/course
ADMIN_IDS=123456789,987654321

# ЮMoney настройки
YOOMONEY_SHOP_ID=410011234567890
YOOMONEY_SECRET_KEY=MyS3cr3tK3y_2025!
PAYMENT_AMOUNT=1000
SERVER_URL=https://yourdomain.com
BOT_USERNAME=your_bot_username

# Настройки сервера
WEBHOOK_PORT=3000
```

**Где взять значения:**

| Переменная | Откуда взять | Пример |
|------------|--------------|--------|
| `YOOMONEY_SHOP_ID` | Номер кошелька из Шага 1.3 | `410011234567890` |
| `YOOMONEY_SECRET_KEY` | Секрет из Шага 2.2 | `MyS3cr3tK3y_2025!` |
| `PAYMENT_AMOUNT` | Стоимость курса в рублях | `1000` |
| `SERVER_URL` | Ваш домен с https:// | `https://mybot.ru` |
| `BOT_USERNAME` | Username бота без @ | `my_english_bot` |

Сохраните: `Ctrl+X` → `Y` → `Enter`

---

### 4.2 Обновите код бота

Локально запушьте изменения:

```bash
# На вашем компьютере
cd "путь/к/проекту"
npm install  # Установит uuid
git add .
git commit -m "Add YooMoney integration"
git push origin main
```

GitHub Actions автоматически задеплоит на сервер.

---

### 4.3 Перезапустите бота

На сервере:

```bash
pm2 restart english-bot
pm2 logs english-bot --lines 20
```

**Должны увидеть:**
```
🤖 Бот запущен!
🌐 Webhook сервер запущен на порту 3000
📍 Webhook URL: https://yourdomain.com/webhook/yoomoney
✅ Success URL: https://yourdomain.com/payment/success
```

---

## 🧪 Шаг 5: Тестирование

### 5.1 Проверка health endpoint

```bash
curl https://yourdomain.com/health
```

**Ожидаемый результат:**
```json
{"status":"ok","bot":"running"}
```

---

### 5.2 Тестовый платёж (минимум 1₽)

1. **Откройте бота в Telegram**
   - Найдите вашего бота
   - Отправьте `/start`

2. **Пройдите воронку**
   - Оставьте контакт
   - Подпишитесь на канал
   - Пройдите до кнопки "Оплатить"

3. **Нажмите "Оплатить 1000₽"**
   - Откроется форма ЮMoney
   - Проверьте сумму и описание

4. **Оплатите тестовую сумму**
   - Можно оплатить минимум 1₽ (измените сумму на форме)
   - Введите данные тестовой карты или используйте кошелёк

5. **После оплаты:**
   - Вас перенаправит на страницу "Оплата прошла успешно!"
   - Вернитесь в Telegram
   - Бот должен автоматически выдать доступ

---

### 5.3 Проверка логов

На сервере посмотрите логи:

```bash
pm2 logs english-bot --lines 50
```

**При успешном платеже должно быть:**
```
Получен webhook от ЮMoney: {...}
✅ Доступ выдан пользователю 123456789, платёж abc-123
```

---

### 5.4 Проверка в ЮMoney

1. Откройте [yoomoney.ru](https://yoomoney.ru/)
2. Перейдите в **"История операций"**
3. Найдите ваш тестовый платёж
4. В деталях должно быть:
   - ✅ Сумма: 1000₽ (или меньше)
   - ✅ Метка: `uuid-строка` (например, `a1b2c3d4-e5f6-...`)
   - ✅ Статус: Успешно

---

## 🛠 Troubleshooting

### ❌ Проблема 1: Webhook не приходит

**Симптомы:**
- Оплата прошла
- Деньги списались
- Но доступ не выдаётся

**Проверка:**

1. **URL доступен из интернета?**
   ```bash
   curl https://yourdomain.com/health
   ```
   Должен вернуть `{"status":"ok","bot":"running"}`

2. **Используется HTTPS (не HTTP)?**
   ```bash
   echo $SERVER_URL
   # Должно быть: https://yourdomain.com (не http!)
   ```

3. **Бот запущен?**
   ```bash
   pm2 status english-bot
   # Должно быть: online
   ```

4. **Правильный URL в настройках ЮMoney?**
   - Откройте [HTTP-уведомления](https://yoomoney.ru/transfer/myservices/http-notification)
   - Проверьте, что URL правильный

5. **Секрет совпадает?**
   ```bash
   grep YOOMONEY_SECRET_KEY .env
   ```
   Должен совпадать с секретом в ЮMoney

**Решение:**

Если webhook всё равно не приходит, используйте **ручной режим**:
```
/paid 123456789
```

---

### ❌ Проблема 2: Ошибка "Invalid signature"

**Логи показывают:**
```
Invalid ЮMoney signature
```

**Причина:** Секрет в `.env` не совпадает с секретом в ЮMoney.

**Решение:**

1. Откройте [HTTP-уведомления](https://yoomoney.ru/transfer/myservices/http-notification)
2. Удалите текущий секрет
3. Придумайте новый (например: `NewSecret123!`)
4. Сохраните в ЮMoney
5. Обновите `.env`:
   ```bash
   nano .env
   # Измените YOOMONEY_SECRET_KEY=NewSecret123!
   ```
6. Перезапустите бота:
   ```bash
   pm2 restart english-bot
   ```

---

### ❌ Проблема 3: "Не найден пользователь для paymentId"

**Логи показывают:**
```
❌ Не найден пользователь для paymentId: abc-123
```

**Причина:**
- Бот перезапускался после создания платежа
- Map с paymentId очистилась

**Решение:**

Выдайте доступ вручную:
```
/paid 123456789
```

Где `123456789` — это Telegram ID пользователя.

**Как узнать ID:**
- Попросите пользователя отправить `/myid`
- Или посмотрите в логах бота

**Долгосрочное решение:**
Используйте базу данных вместо Map (Redis, MongoDB, PostgreSQL).

---

### ❌ Проблема 4: SSL сертификат не работает

**Ошибка:**
```
curl: (60) SSL certificate problem
```

**Проверка:**
```bash
curl -I https://yourdomain.com
```

**Решение:**

1. Проверьте Certbot:
   ```bash
   sudo certbot certificates
   ```

2. Обновите сертификат:
   ```bash
   sudo certbot renew
   ```

3. Проверьте Nginx:
   ```bash
   sudo nginx -t
   sudo systemctl reload nginx
   ```

---

### ❌ Проблема 5: Форма оплаты не открывается

**Симптомы:**
- Нажимаю "Оплатить"
- Ничего не происходит

**Проверка логов:**
```bash
pm2 logs english-bot | grep "payment"
```

**Решение:**

1. Проверьте `YOOMONEY_SHOP_ID` в `.env`:
   ```bash
   grep YOOMONEY_SHOP_ID .env
   # Должно быть 15 цифр, начинающихся с 4100
   ```

2. Перезапустите бота:
   ```bash
   pm2 restart english-bot
   ```

---

### ❌ Проблема 6: ЮMoney не принимает мой webhook URL

**Требования ЮMoney:**
- ✅ Должен начинаться с `https://`
- ✅ Должен быть доступен из интернета (не localhost!)
- ✅ Должен возвращать статус 200 OK при POST запросе
- ✅ Порт только 443 (стандартный HTTPS)

**Проверка:**
```bash
curl -X POST https://yourdomain.com/webhook/yoomoney \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "test=1"
```

Должен вернуть `OK` (или любой 200 статус).

---

## 💰 Комиссии и лимиты

### Комиссии ЮMoney (актуально на 2025)

| Способ оплаты | Комиссия для получателя |
|---------------|-------------------------|
| Из кошелька ЮMoney | **0.5%** + 0₽ |
| Банковская карта | **2%** + 0₽ |
| Apple Pay / Google Pay | **2%** + 0₽ |

**Пример расчёта:**
- Пользователь платит: 1000₽
- Вы получаете (карта): 980₽ (комиссия 20₽)
- Вы получаете (кошелёк): 995₽ (комиссия 5₽)

### Лимиты для физлиц

| Статус кошелька | Лимит на приём |
|-----------------|----------------|
| Анонимный | ❌ Нельзя принимать |
| Именной | ✅ До 15,000₽/месяц |
| Идентифицированный | ✅ До 500,000₽/месяц |

**💡 Совет:** Идентифицируйтесь сразу, чтобы не было проблем с лимитами.

---

## 🔐 Безопасность

### Чек-лист безопасности:

- ✅ Используйте сложный секрет для webhook
- ✅ Всегда проверяйте подпись (sha1_hash) — бот уже делает это
- ✅ Используйте HTTPS (обязательно!)
- ✅ Не коммитьте `.env` в git (уже в .gitignore)
- ✅ Регулярно проверяйте логи: `pm2 logs english-bot`
- ✅ Используйте файрвол: `sudo ufw enable`
- ✅ Обновляйте сервер: `sudo apt update && sudo apt upgrade`

### Генерация надёжного секрета:

```bash
openssl rand -base64 32
# Используйте результат в YOOMONEY_SECRET_KEY
```

---

## 📊 Мониторинг и аналитика

### Логирование всех платежей

Добавьте в [src/bot.ts](src/bot.ts) уведомления админам:

```typescript
// В webhook endpoint после handleSuccessfulPayment
const adminIds = process.env.ADMIN_IDS?.split(',').map(id => parseInt(id)) || [];
for (const adminId of adminIds) {
  await bot.sendMessage(
    adminId,
    `💰 Новый платёж!\n\n` +
    `👤 Пользователь: ${chatId}\n` +
    `💵 Сумма: ${amount} ${currency}\n` +
    `🆔 ID операции: ${operation_id}\n` +
    `📅 Дата: ${datetime}`
  );
}
```

### Сохранение платежей в файл

```bash
pm2 logs english-bot | grep "Получен платёж" >> payments.log
```

---

## ❓ Частые вопросы (FAQ)

### 1. Нужно ли регистрировать ИП/ООО?

**Нет!** Физлица могут принимать платежи через ЮMoney без регистрации бизнеса.

**Но:**
- Нужна идентификация кошелька
- Есть лимит 500,000₽/месяц
- Платятся налоги (декларируйте доход)

### 2. Можно ли принимать платежи из-за рубежа?

**Да**, ЮMoney принимает международные карты Visa/MasterCard.

**Но:**
- Не все карты поддерживаются
- Комиссия та же (2%)
- Могут быть ограничения от банков

### 3. Как изменить сумму платежа?

В `.env` измените:
```env
PAYMENT_AMOUNT=2000
```

И перезапустите бота:
```bash
pm2 restart english-bot
```

### 4. Можно ли сделать несколько тарифов?

**Да!** Нужно доработать код бота.

Пример в [src/bot.ts](src/bot.ts):
```typescript
case 'show_product':
  // Показываем разные тарифы
  await bot.sendMessage(chatId, 'Выберите тариф:', {
    reply_markup: {
      inline_keyboard: [
        [{ text: '💳 Базовый - 1000₽', callback_data: 'payment_1000' }],
        [{ text: '💎 Премиум - 2000₽', callback_data: 'payment_2000' }],
        [{ text: '👑 VIP - 5000₽', callback_data: 'payment_5000' }],
      ]
    }
  });
```

### 5. Webhook приходит с задержкой

ЮMoney отправляет уведомления в течение **1-5 минут** после оплаты. Это нормально.

Если дольше — проверьте настройки и логи.

### 6. Как получить API токен ЮMoney?

Quick Pay **не требует** API токен! Всё работает через форму оплаты.

Токен нужен только для работы через API (проверка баланса, история операций и т.д.).

### 7. Что делать, если кошелёк заблокировали?

Свяжитесь с поддержкой ЮMoney: [yoomoney.ru/page?id=524857](https://yoomoney.ru/page?id=524857)

Частые причины блокировки:
- Подозрительная активность
- Нарушение правил (продажа запрещённых товаров)
- Жалобы пользователей

### 8. Можно ли вернуть деньги пользователю?

**Да**, через личный кабинет ЮMoney:
1. История операций
2. Найдите платёж
3. Нажмите "Вернуть"

Или вручную переведите деньги обратно.

---

## 🎯 Чек-лист готовности

Перед запуском проверьте:

### Настройки ЮMoney:
- ✅ Кошелёк создан
- ✅ Идентификация пройдена
- ✅ HTTP-уведомления настроены
- ✅ URL правильный (https://)
- ✅ Секрет сохранён

### Настройки сервера:
- ✅ Домен настроен
- ✅ SSL сертификат установлен
- ✅ Nginx настроен
- ✅ Порт 443 открыт
- ✅ Health endpoint работает

### Настройки бота:
- ✅ `.env` заполнен
- ✅ Бот запущен: `pm2 status`
- ✅ Логи чистые: `pm2 logs`
- ✅ Webhook URL выводится в логах

### Тестирование:
- ✅ Тестовый платёж прошёл
- ✅ Webhook пришёл
- ✅ Доступ выдался автоматически
- ✅ Страница успеха открылась

---

## 🎉 Готово!

Поздравляю! Теперь ваш бот принимает платежи через ЮMoney с автоматической выдачей доступа!

### Что дальше?

1. **Протестируйте на реальных пользователях**
2. **Настройте мониторинг** (уведомления в Telegram)
3. **Подключите базу данных** (вместо Map)
4. **Добавьте аналитику** (сколько платежей, конверсия)
5. **Масштабируйте!** 🚀

### Полезные ссылки:

- 📖 Документация ЮMoney: [yoomoney.ru/docs/wallet](https://yoomoney.ru/docs/wallet)
- 💬 Поддержка ЮMoney: [yoomoney.ru/page?id=524857](https://yoomoney.ru/page?id=524857)
- 🤖 Ваш бот: `https://t.me/your_bot_username`

---

**Остались вопросы?** Проверьте раздел [Troubleshooting](#troubleshooting) или логи бота:
```bash
pm2 logs english-bot --lines 100
```

**Удачи с запуском! 💪**
