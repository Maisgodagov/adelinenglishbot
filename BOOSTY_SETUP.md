# Настройка интеграции с Boosty

Полная инструкция по подключению приёма платежей через Boosty к вашему боту.

## 🚀 Быстрый старт (TL;DR)

1. **Создайте "Цель" на Boosty** на сумму 1000₽
2. **Скопируйте ссылку** на цель: `https://boosty.to/yourpage/goals/12345`
3. **Добавьте в `.env`:**
   ```env
   BOOSTY_PAYMENT_LINK=https://boosty.to/yourpage/goals/12345
   ```
4. **Задеплойте бота:** `git push origin main`
5. **Выдавайте доступ вручную:** `/paid USER_ID` (ID из комментария к платежу)
6. **(Опционально)** Настройте webhook для автоматической выдачи доступа

---

## 📋 Что такое Boosty?

[Boosty](https://boosty.to/) — российская платформа для монетизации контента. Поддерживает приём платежей картами РФ и из-за рубежа.

**Преимущества:**
- ✅ Работает с российскими картами
- ✅ Низкая комиссия (10%)
- ✅ Поддержка webhook для автоматизации (не у всех аккаунтов)
- ✅ Простая интеграция
- ✅ Можно установить фиксированную сумму через "Цели"

---

## 🚀 Шаг 1: Создание Цели на Boosty (актуальная инструкция)

### 1.1 Зарегистрируйтесь на Boosty

1. Перейдите на [boosty.to](https://boosty.to/)
2. Нажмите "Войти" → зарегистрируйтесь через Google/VK/Email
3. Заполните профиль (имя, описание, аватар)

### 1.2 Создайте Цель для разового платежа на 1000₽

**Важно:** Используем "Цель", а не донаты, так как цель позволяет установить фиксированную сумму.

1. **Откройте ваш профиль** на Boosty: `https://boosty.to/yourpage`

2. **Нажмите на три точки** (⋮) справа сверху → **"Создать цель"**

3. **Заполните данные цели:**
   - **Название:** "Букварь английского языка" (или как хотите)
   - **Описание:**
     ```
     Полный курс "Букварь английского" - 30+ аудиоуроков для изучения английского языка.

     После оплаты вы получите:
     ✅ Доступ к 30+ урокам
     ✅ Аудио-файлы для произношения
     ✅ Примеры и ассоциации
     ✅ Пожизненный доступ
     ```
   - **Сумма цели:** `1000` ₽
   - **Можно ли собрать больше:** Включите, если хотите принимать больше 1000₽
   - **Опубликовать сейчас:** Да

4. **Нажмите "Создать"**

5. **Скопируйте ссылку на цель:**
   - Откройте созданную цель
   - Ссылка будет вида: `https://boosty.to/yourpage/goals/12345`
   - Или нажмите на цель → кнопка "Поделиться" → скопируйте ссылку

### 1.3 Альтернатива: Создайте продукт (товар)

Если "Цели" не подходят, можно создать цифровой продукт:

1. **Перейдите в "Продукты"** (Магазин)
2. **Нажмите "Создать продукт"**
3. **Заполните:**
   - Название: "Букварь английского"
   - Цена: 1000₽
   - Тип: Цифровой товар
   - Описание курса
4. **Сохраните**
5. **Скопируйте ссылку на продукт**

### 1.4 Добавьте ссылку в .env

Откройте `.env` и добавьте ссылку на вашу цель или продукт:

```env
# Ссылка на цель
BOOSTY_PAYMENT_LINK=https://boosty.to/yourpage/goals/12345

# Или ссылка на продукт
BOOSTY_PAYMENT_LINK=https://boosty.to/yourpage/shop/12345
```

**Важно:** Замените `yourpage` на ваш никнейм в Boosty и `12345` на ID цели/продукта!

---

## 🔔 Шаг 2: Настройка Webhook (автоматическая выдача доступа)

Webhook позволяет получать уведомления о платежах автоматически.

### 2.1 Получите URL вашего webhook

После деплоя бота на сервер, ваш webhook будет доступен по адресу:
```
http://your-server-ip:3000/webhook/boosty
```

Или с доменом (рекомендуется):
```
https://yourdomain.com/webhook/boosty
```

**Важно:** Boosty требует HTTPS для webhook! Используйте домен с SSL сертификатом.

### 2.2 Настройте webhook в Boosty (актуальная инструкция)

**Способ 1: Через настройки блога**

1. Войдите в Boosty: [boosty.to](https://boosty.to/)
2. Откройте свой профиль
3. Нажмите **"Настройки"** (значок шестерёнки)
4. В меню слева выберите **"Интеграции"** или **"Webhooks"**
5. Нажмите **"Добавить webhook"** или **"Настроить webhook"**
6. Заполните форму:
   - **URL:** `https://yourdomain.com/webhook/boosty`
   - **События:** Отметьте "Новый платёж" или "Donation" или "Payment completed"
   - **Секретный ключ:** Создайте надёжный пароль (например, `mySecretKey123!`)
7. **Сохраните**

**Способ 2: Через API (если webhook в интерфейсе недоступен)**

Если в интерфейсе нет webhook, придётся использовать альтернативный подход:

1. **Вариант A:** Используйте интеграцию через email
   - При каждом платеже Boosty присылает письмо
   - Настройте автоматическую пересылку на сервер

2. **Вариант B:** Проверка через API
   - Используйте Boosty API для периодической проверки новых платежей
   - Требуется получить API токен

3. **Вариант C (самый простой):** Ручное подтверждение
   - Используйте команду `/paid USER_ID` для выдачи доступа
   - Подходит для небольшого количества платежей

### 2.3 Добавьте секретный ключ в .env

Откройте `.env` и добавьте секретный ключ, который указали в webhook:

```env
BOOSTY_WEBHOOK_SECRET=mySecretKey123!
```

**Важно:** Этот ключ должен совпадать с тем, что вы указали в настройках Boosty!

### 2.4 Если webhook недоступен в вашем аккаунте

К сожалению, Boosty может не предоставлять webhook для всех аккаунтов. В этом случае:

**Решение 1: Используйте ручное подтверждение**

Когда пользователь оплатил, вы получите уведомление на email. Затем выполните:
```
/paid 123456789
```
Где `123456789` — это Telegram ID пользователя (будет в комментарии к платежу).

**Решение 2: Интеграция через Telegram bot @notificator**

Некоторые сервисы предоставляют уведомления о платежах Boosty в Telegram. Найдите такой бот и настройте пересылку.

**Решение 3: Используйте альтернативную платёжную систему**

- **ЮKassa (YooKassa)** - официально поддерживает webhook
- **Telegram Payments** - встроенная оплата прямо в боте
- **Робокасса** - для ИП/ООО

---

## 🌐 Шаг 3: Настройка сервера

### 3.1 Откройте порт для webhook

Бот использует порт 3000 по умолчанию. Откройте его в файрволе:

```bash
# На Ubuntu/Debian
sudo ufw allow 3000/tcp

# Проверьте статус
sudo ufw status
```

### 3.2 (Рекомендуется) Настройте Nginx как reverse proxy

Создайте конфиг Nginx:

```bash
sudo nano /etc/nginx/sites-available/bot-webhook
```

Вставьте:
```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location /webhook/boosty {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }

    location /health {
        proxy_pass http://localhost:3000;
    }
}
```

Активируйте конфиг:
```bash
sudo ln -s /etc/nginx/sites-available/bot-webhook /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 3.3 (Рекомендуется) Настройте SSL с Certbot

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com
```

Теперь webhook доступен по HTTPS: `https://yourdomain.com/webhook/boosty`

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверьте, что webhook сервер запущен

```bash
# На сервере
curl http://localhost:3000/health
# Должен вернуть: {"status":"ok","bot":"running"}
```

### 4.2 Проверьте логи бота

```bash
pm2 logs english-bot
```

Должна быть строка:
```
🌐 Webhook сервер запущен на порту 3000
```

### 4.3 Сделайте тестовый платёж

1. Запустите бота: `/start`
2. Пройдите воронку до оплаты
3. Нажмите "Оплатить через Boosty"
4. Сделайте тестовый платёж (можно минимальную сумму)
5. Проверьте, что доступ пришёл автоматически

### 4.4 Проверьте webhook в Boosty

В настройках Boosty → Webhooks → История вызовов:
- Статус должен быть `200 OK`
- Если ошибка — проверьте логи: `pm2 logs english-bot`

---

## 🔧 Как работает интеграция

### Процесс оплаты (с webhook):

1. **Пользователь нажимает "Оплатить"**
   - Бот генерирует персональную ссылку с `comment=USER_123456`
   - Пользователь переходит на Boosty

2. **Пользователь оплачивает на Boosty**
   - Boosty принимает платёж
   - В комментарии к платежу автоматически сохраняется `USER_123456`

3. **Boosty отправляет webhook (если настроен)**
   - POST запрос на `https://yourdomain.com/webhook/boosty`
   - В теле: информация о платеже + комментарий с USER_ID

4. **Бот обрабатывает webhook**
   - Извлекает `chatId` из комментария
   - Проверяет подпись (если настроена)
   - Вызывает `handleSuccessfulPayment(chatId)`

5. **Пользователь получает доступ автоматически**
   - Бот отправляет ссылку на курс
   - Пользователь видит сообщение об успешной оплате

---

### Процесс оплаты (БЕЗ webhook - ручной режим):

Если webhook не настроен или недоступен, используйте ручное подтверждение:

1. **Пользователь оплачивает**
   - Нажимает "Оплатить через Boosty"
   - Переходит по ссылке и оплачивает
   - В комментарии к платежу автоматически указан его ID: `USER_123456789`

2. **Вы получаете уведомление**
   - Boosty присылает email о новом платеже
   - В письме будет комментарий: `USER_123456789`

3. **Выдаёте доступ вручную**
   - Откройте Telegram
   - Отправьте боту команду: `/paid 123456789`
   - Бот автоматически выдаст доступ пользователю

**Пример:**
```
# Вы получили письмо от Boosty:
# "Новый платёж 1000₽ от Ivan. Комментарий: USER_987654321"

# Отправляете боту:
/paid 987654321

# Бот ответит:
✅ Доступ выдан пользователю 987654321
```

---

### Как пользователь узнаёт свой ID?

**Автоматически в ссылке:**
Когда пользователь нажимает "Оплатить через Boosty", бот генерирует ссылку вида:
```
https://boosty.to/yourpage/goals/12345?comment=USER_123456789
```

Параметр `comment=USER_123456789` автоматически подставляется в комментарий к платежу на Boosty.

**Дополнительно:** Можно добавить в бота команду для показа ID:
- Пользователь отправляет `/myid`
- Бот отвечает: "Ваш ID: 123456789"

---

## 🛠 Troubleshooting

### ❌ Webhook не приходит

**Проверьте:**
1. Порт 3000 открыт: `sudo ufw status`
2. Бот запущен: `pm2 status english-bot`
3. Webhook сервер работает: `curl http://localhost:3000/health`
4. URL правильный в настройках Boosty
5. Логи: `pm2 logs english-bot --lines 50`

### ❌ Ошибка "Invalid signature"

**Решение:**
1. Проверьте, что `BOOSTY_WEBHOOK_SECRET` совпадает с секретом в Boosty
2. Или уберите секрет из .env (небезопасно, но для теста подойдёт)

### ❌ Доступ не выдаётся после оплаты

**Проверьте:**
1. Логи: `pm2 logs english-bot`
2. Webhook пришёл в Boosty (История вызовов)
3. В комментарии к платежу есть `USER_12345`
4. chatId корректно извлекается из комментария

**Решение:**
```bash
# Проверьте логи на сервере
pm2 logs english-bot --lines 100 | grep "Получен платёж"
```

### ❌ Платёж прошёл, но бот не отреагировал

**Ручная выдача доступа:**
```
/paid 123456789
```
Где `123456789` — ID пользователя (можно найти в логах или попросить написать боту `/start`)

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте HTTPS** (настройте SSL через Certbot)
2. **Настройте webhook secret** в Boosty и добавьте в `.env`
3. **Ограничьте доступ к webhook** через Nginx
4. **Логируйте все платежи** для отслеживания проблем
5. **Регулярно проверяйте логи**: `pm2 logs english-bot`

### Настройка дополнительной защиты:

В Nginx добавьте ограничение по IP (если Boosty использует статические IP):
```nginx
location /webhook/boosty {
    allow 123.45.67.89;  # IP Boosty
    deny all;
    proxy_pass http://localhost:3000;
}
```

---

## 📊 Мониторинг платежей

### Добавьте уведомления админам о платежах

Отредактируйте [src/bot.ts](src/bot.ts), найдите функцию обработки webhook и добавьте:

```typescript
// После строки: await handleSuccessfulPayment(chatId);

// Уведомление админу
const adminIds = process.env.ADMIN_IDS?.split(',').map(id => parseInt(id)) || [];
for (const adminId of adminIds) {
  await bot.sendMessage(
    adminId,
    `💰 Новый платёж!\n\n` +
    `От: ${chatId}\n` +
    `Сумма: ${amount}₽\n` +
    `Email: ${email || 'не указан'}`
  );
}
```

---

## 💡 Альтернативы Boosty

Если Boosty не подходит, можно использовать:

### YooKassa (ЮKassa)
- Официальная платёжная система от Яндекса
- Более надёжная, но требует регистрации ИП/ООО
- [Инструкция по интеграции](https://yookassa.ru/developers)

### Telegram Payments
- Встроенная система оплаты в Telegram
- Работает через провайдеров (YooKassa, Stripe и др.)
- Оплата прямо в боте без перехода на сайт

### Stripe
- Международные платежи
- Не работает с российскими картами без VPN
- Требует регистрации компании

---

## 🎯 Готово!

Теперь ваш бот принимает платежи через Boosty! 🎉

### Что происходит (с webhook):
1. Пользователь нажимает "Оплатить" → переходит на Boosty
2. Оплачивает курс (в комментарии автоматически `USER_12345`)
3. Boosty отправляет webhook боту
4. Бот автоматически выдаёт доступ

### Что происходит (без webhook):
1. Пользователь нажимает "Оплатить" → переходит на Boosty
2. Оплачивает курс (в комментарии автоматически `USER_12345`)
3. Вы получаете email от Boosty с комментарием
4. Отправляете боту: `/paid 12345`
5. Бот выдаёт доступ пользователю

---

## ❓ Частые вопросы (FAQ)

### 1. Как установить фиксированную сумму 1000₽?

Используйте "Цели" вместо донатов:
- Создайте цель на 1000₽
- Пользователь будет оплачивать именно эту сумму
- Альтернатива: создайте "Продукт" с ценой 1000₽

### 2. Где найти "Принимать донаты"?

В текущей версии Boosty функция "Донаты" может называться по-другому или быть недоступна для новых аккаунтов. **Используйте "Цели" или "Продукты"** вместо донатов.

### 3. Как настроить webhook, если не вижу его в настройках?

Webhook доступен не для всех аккаунтов Boosty. Если не видите webhook:
- Используйте ручное подтверждение: `/paid USER_ID`
- Или напишите в поддержку Boosty с просьбой включить webhook

### 4. Как узнать ID пользователя для `/paid`?

ID пользователя указан в комментарии к платежу на Boosty:
- Формат: `USER_123456789`
- Используйте только цифры: `/paid 123456789`
- Пользователь может узнать свой ID командой `/myid`

### 5. Можно ли принимать оплату картами из-за рубежа?

Да, Boosty поддерживает международные карты. Но проверьте ограничения для вашего аккаунта.

### 6. Сколько Boosty берёт комиссию?

Boosty берёт 10% от суммы платежа. То есть с 1000₽ вы получите 900₽.

### 7. Webhook не работает, что делать?

**Проверьте:**
1. Webhook сервер запущен: `pm2 status english-bot`
2. Порт открыт: `sudo ufw status`
3. URL правильный (должен быть HTTPS!)
4. Секретный ключ совпадает в `.env` и Boosty
5. Логи: `pm2 logs english-bot --lines 50`

**Если не помогает:**
Используйте ручной режим с `/paid`

### 8. Как сделать, чтобы пользователь мог оплатить больше 1000₽?

При создании цели включите опцию "Можно ли собрать больше". Пользователь сможет указать любую сумму >= 1000₽.

### 9. Как проверить, что интеграция работает?

1. Запустите бота: `/start`
2. Пройдите воронку до оплаты
3. Нажмите "Оплатить через Boosty"
4. Проверьте, что ссылка содержит `?comment=USER_`
5. Сделайте тестовый платёж (минимальная сумма)
6. Проверьте логи: `pm2 logs english-bot`

### 10. Бот упал после добавления Boosty, что делать?

**Проверьте зависимости:**
```bash
npm install
npm run build
pm2 restart english-bot
pm2 logs english-bot
```

**Частые причины:**
- Не установлен `express`: `npm install express`
- Ошибка в `.env` файле
- Порт 3000 уже занят (измените `WEBHOOK_PORT`)

---

**Проверьте перед запуском:**
- ✅ Цель создана на Boosty
- ✅ Ссылка добавлена в `.env`
- ✅ Зависимости установлены: `npm install`
- ✅ Бот собран: `npm run build`
- ✅ Бот запущен: `pm2 status`
- ✅ Логи чистые: `pm2 logs english-bot`

**Поддержка:**
- Логи бота: `pm2 logs english-bot`
- Health check: `curl http://localhost:3000/health`
- Статус: `pm2 status english-bot`
- Перезапуск: `pm2 restart english-bot`
