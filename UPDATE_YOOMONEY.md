# Обновление: Замена Boosty на ЮMoney

Краткая инструкция по обновлению бота с Boosty на ЮMoney.

## 🚀 Быстрое обновление

### 1. Зарегистрируйтесь в ЮMoney

1. Создайте кошелёк на [yoomoney.ru](https://yoomoney.ru/)
2. Пройдите идентификацию (обязательно!)
3. Настройте HTTP-уведомления в личном кабинете

📖 **Подробная инструкция:** [YOOMONEY_SETUP.md](YOOMONEY_SETUP.md)

### 2. Обновите .env на сервере

```bash
ssh your-user@your-server-ip
cd ~/english-bot
nano .env
```

**Удалите старые переменные Boosty:**
```env
# Удалите эти строки:
BOOSTY_PAYMENT_LINK=...
BOOSTY_WEBHOOK_SECRET=...
```

**Добавьте новые переменные ЮMoney:**
```env
# ЮMoney настройки
YOOMONEY_SHOP_ID=410011234567890
YOOMONEY_SECRET_KEY=your_secret_key
PAYMENT_AMOUNT=1000
SERVER_URL=https://yourdomain.com
BOT_USERNAME=your_bot_username
WEBHOOK_PORT=3000
```

Сохраните: `Ctrl+X`, `Y`, `Enter`

### 3. Запушьте изменения (локально)

```bash
npm install  # Установит uuid
git add .
git commit -m "Replace Boosty with YooMoney"
git push origin main
```

GitHub Actions автоматически задеплоит на сервер.

### 4. Проверьте, что всё работает

```bash
ssh your-user@your-server-ip
pm2 logs english-bot
```

Должна быть строка:
```
🤖 Бот запущен!
🌐 Webhook сервер запущен на порту 3000
📍 Webhook URL: https://yourdomain.com/webhook/yoomoney
✅ Success URL: https://yourdomain.com/payment/success
```

### 5. Настройте webhook в ЮMoney

1. Откройте [HTTP-уведомления в ЮMoney](https://yoomoney.ru/transfer/myservices/http-notification)
2. Введите URL: `https://yourdomain.com/webhook/yoomoney`
3. Придумайте секрет (тот же что в `.env`)
4. Сохраните

**⚠️ Важно:** URL должен быть HTTPS! HTTP не поддерживается.

---

## 🔍 Проверка

### Проверьте webhook:
```bash
curl https://yourdomain.com/health
```

Должен вернуть:
```json
{"status":"ok","bot":"running"}
```

### Проверьте страницу успеха:
```bash
curl https://yourdomain.com/payment/success
```

Должна вернуть HTML страницу.

### Проверьте логи:
```bash
pm2 logs english-bot --lines 50
```

---

## 🧪 Тестирование

1. Запустите бота: `/start`
2. Пройдите до оплаты
3. Нажмите "Оплатить через ЮMoney"
4. Должна открыться форма оплаты ЮMoney
5. Оплатите 1₽ (тестовый платёж)
6. После оплаты вас перенаправит на страницу успеха
7. Вернитесь в Telegram — бот должен выдать доступ

---

## ❌ Если что-то пошло не так

### Бот не запускается:

```bash
cd ~/english-bot
npm install
npm run build
pm2 restart english-bot
pm2 logs english-bot
```

### Ошибка "Cannot find module 'uuid'":

```bash
cd ~/english-bot
npm install uuid
npm run build
pm2 restart english-bot
```

### Webhook не работает:

1. **Проверьте URL:**
   ```bash
   curl https://yourdomain.com/health
   ```

2. **Проверьте SSL:**
   ЮMoney требует HTTPS!

3. **Проверьте секрет в .env:**
   Должен совпадать с секретом в ЮMoney

4. **Проверьте логи:**
   ```bash
   pm2 logs english-bot --lines 100
   ```

### Используйте ручной режим:

Если webhook не работает, выдавайте доступ вручную:

1. Получите уведомление о платеже на email
2. Найдите Telegram ID пользователя (он в логах или попросите написать `/myid`)
3. Отправьте боту: `/paid 123456789`

---

## 🔄 Откат на Boosty (если нужно)

Если ЮMoney не подошёл, можно вернуться на Boosty:

```bash
git revert HEAD
git push origin main
```

И восстановите настройки Boosty в `.env`.

---

## 📊 Основные отличия

| Функция | Boosty | ЮMoney |
|---------|--------|--------|
| Комиссия | 10% | 2% (карты) / 0.5% (кошелёк) |
| Webhook | Не у всех | Есть у всех |
| Требования | Нет | Идентификация кошелька |
| SSL | Опционально | Обязательно |
| Фиксированная сумма | Через "Цели" | Да, в параметрах |

---

## ✅ Готово!

Теперь бот работает с ЮMoney вместо Boosty! 🎉

**Преимущества:**
- ✅ Ниже комиссия
- ✅ Webhook у всех пользователей
- ✅ Моментальное зачисление
- ✅ Проще настроить

**Тестирование:**
- Проверьте `/start` → оплату
- Сделайте тестовый платёж
- Убедитесь, что доступ выдаётся автоматически

**Поддержка:**
- Логи: `pm2 logs english-bot`
- Health: `curl https://yourdomain.com/health`
- ЮMoney: [yoomoney.ru](https://yoomoney.ru/)

Полная инструкция в [YOOMONEY_SETUP.md](YOOMONEY_SETUP.md)! 📚
